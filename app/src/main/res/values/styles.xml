<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor" tools:targetApi="lollipop">@color/colorPrimaryDark
        </item>

    </style>

    <style name="AppTheme.WithActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor" tools:targetApi="lollipop">@color/colorPrimaryDark
        </item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- Styles for article layout attributes. -->
    <style name="articleAttributes">
        <item name="android:layout_marginEnd" tools:targetApi="jelly_bean_mr1">
            @dimen/article_marginEnd
        </item>
        <item name="android:layout_marginStart" tools:targetApi="jelly_bean_mr1">
            @dimen/article_marginStart
        </item>
        <item name="android:layout_marginTop">@dimen/article_marginTop</item>
    </style>

    <!-- Styles for common attributes of all 4 slides layouts of ImageView. -->
    <style name="slideImageAttributes">
        <item name="android:layout_above">@id/tutorial_title</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_marginBottom">@dimen/slide_icon_marginBottom</item>
        <item name="android:layout_width">@dimen/img_width_height</item>
        <item name="android:layout_height">@dimen/img_width_height</item>
    </style>

    <!-- Styles for common attributes of all 4 slides layouts of Title textView. -->
    <style name="slideTitleAttributes">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="android:textAlignment" tools:targetApi="jelly_bean_mr1">center</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/slide_title_textSize</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Styles for common attributes of all 4 slides layouts of Description textView. -->
    <style name="slideDescAttributes">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_below">@id/tutorial_title</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_marginTop">@dimen/slide_desc_marginTop</item>
        <item name="android:paddingLeft">@dimen/desc_padding</item>
        <item name="android:paddingRight">@dimen/desc_padding</item>
        <item name="android:textAlignment" tools:targetApi="jelly_bean_mr1">center</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/slide_desc_textSize</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@xml/splash_background</item>
    </style>

</resources>
